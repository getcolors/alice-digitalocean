# Default-deny dotfiles so credentials, generated state, and newly introduced
# private files cannot be committed accidentally.
.*

# Safe project metadata explicitly admitted to source control.
!.envrc
!.gitignore
!.nojekyll
!.agents
!.claude
!.github

# Explicitly document the sensitive/generated paths covered by the default.
/.envrc.private
/.colors/

node_modules/
